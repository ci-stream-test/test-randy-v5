version: v2.0
name: 测试归档构件

steps:
  - run: |
      rm -rf  ./report
      mkdir ./report
      echo "test upload report" > ./report/report.txt
      ls -a
  - uses: uploadArtifact@1.*
    name: 归档构件
    with:
       fileDir: "./report"
       repoName: "pipeline"
       isParentPipeline: false 
